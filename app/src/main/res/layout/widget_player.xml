<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <!--
      音符瀑布作为背景层铺满整个部件，而不是挤在文字下面单独占一行。
      这样用户把部件拉成多高多扁都能填满，也不会因为固定占位
      把小部件本来就不多的高度吃光。
      没有音符（音频 / 模块曲目）时整层 GONE，布局退回原来的样子。
    -->
    <ImageView
        android:id="@+id/widget_waterfall"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/widget_waterfall"
        android:scaleType="fitXY"
        android:visibility="gone" />

    <!-- 压暗那层直接烤进了位图，这里不再多放一个 View（少一处 inflate 失败的可能） -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:padding="14dp">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:shadowColor="#CC000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="3"
            android:text="@string/widget_no_track"
            android:textColor="#FFFFFFFF"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:shadowColor="#CC000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="3"
            android:text="@string/widget_subtitle_placeholder"
            android:textColor="#9A98A0"
            android:textSize="12sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/widget_prev"
                android:layout_width="46dp"
                android:layout_height="46dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/widget_prev"
                android:scaleType="centerInside"
                android:src="@drawable/widget_prev" />

            <ImageButton
                android:id="@+id/widget_toggle"
                android:layout_width="54dp"
                android:layout_height="54dp"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/widget_toggle"
                android:scaleType="centerInside"
                android:src="@drawable/widget_play" />

            <ImageButton
                android:id="@+id/widget_next"
                android:layout_width="46dp"
                android:layout_height="46dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/widget_next"
                android:scaleType="centerInside"
                android:src="@drawable/widget_next" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
